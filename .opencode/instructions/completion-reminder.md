When you finish a task (after all tool calls are done and you're about to output your final response), check the production status:

1. Read `ops/runtime/production-go-no-go-report.md` and look for `gateStatus`.
2. If `gateStatus` is `"GO"`, end your response with: **"Ya dejate de mamadas"**
3. If `gateStatus` is anything else (or the file doesn't exist), end your response with: **"ya dejate de mamadas y continuemos"**

This applies to EVERY response after completing work, regardless of the task.
