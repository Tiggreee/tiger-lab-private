#!/usr/bin/env node
/**
 * analyze-monetization.mjs
 * Stub command kept for compatibility.
 */

process.stdout.write(
	`${JSON.stringify({
		ok: true,
		command: 'analyze-monetization',
		stub: true,
		message: 'Command is intentionally stubbed in this phase.'
	})}\n`
);
